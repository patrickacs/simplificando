// Este arquivo está vazio intencionalmente
// As page transitions são gerenciadas pelo ClientWrapper
export default function Template({ children }: { children: React.ReactNode }) {
    return <>{children}</>;
}
